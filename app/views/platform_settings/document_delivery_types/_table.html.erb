<table class="min-w-full divide-y divide-gray-300">
  <thead class="bg-gray-50">
    <tr>
      <%= render "shared/table/sort_table_header", query: @query, column: "description", current_sort: current_sort %>
      <%= render "shared/table/sort_table_header", query: @query, column: "is_active", current_sort: current_sort %>      
      <%= render "shared/table/sort_table_header", query: @query, column: "guid", current_sort: current_sort %>           

    </tr>
    <tr>
      <%= render "shared/table/search_table_header", form: form, search_field: "description_cont" %>
      <%= render "shared/table/search_table_header", form: form, search_field: "is_active_cont" %>   
      <%= render "shared/table/search_table_header", form: form, search_field: "guid_eq" %>         

    </tr>    
  </thead>

  <tbody class="divide-y divide-gray-200 bg-white">
    <% @platform_document_delivery_types.each do |platform_document_delivery_type| %>
      <%= render "platform_document_delivery_type", platform_document_delivery_type: platform_document_delivery_type %>
    <% end %>
  </tbody>
</table>
